# Project 6 実装記録

対象: https://github.com/users/aoyaman41/projects/6 （親Issue #70、#71–#92、#65）

2026-09-05。全Issueの受け入れ条件を満たすまで実装・検証する。未確認項目を完了としない。

## 基準点

- ローカル既存変更を4ca83f7で保全。最新main bebb4e4をfdee588で統合。
- Astra / GPT-5.6の設定・使用量対応を維持し、mainのGPT-5.5、画像参照、Gemini TTS、ネイティブ動画を統合。
- 統合後112テスト、lint、Main/Renderer型チェックを通過。

## 保存基盤（実装中、Issueは未完了）

- MainのProjectRepository: 単一manifestのfsync→rename、完全な直前世代、プロジェクト単位の直列化、revision競合拒否。
- 旧v1.0–v1.2は読み込み、保存でv2.0へ移行。破損・未対応形式・不存在を識別。
- shared/project/schema.tsをMain/Rendererで共有。生成前の空の記事・台本も保存可能。
- Renderer共有ストア: 400msの保存キュー、入力中の追記保持、保存失敗時の再試行、終了時flush。
- 記事・取り込み素材・台本入力を即時共有状態へ反映。画像Fileのバイト列をMainへ送りコピーを永続化。
- Sidebar/WorkflowNavの定期ポーリングを除去し、共有状態・変更イベントへ接続。
- 削除はゴミ箱への移動へ変更（復元UIは#82で後続実装）。
- 119テスト（競合、最終rename失敗、破損復旧、移行、保存失敗、保存中の追記）、lint、型チェック、Vite build通過。最終画像取込変更後の型チェック・テスト・lintも通過。
- UIでの編集→即移動→再起動、終了時保存、故障時の表示はまだ実機検証が必要。

## 残作業

1. 保存実機検証、生成物の入力版・ファイル実在確認（#73）、競合の差分確認・再読込UI（#85）。
2. Main永続ジョブ、共通の並列数/エラー/リトライ/取消、予算、権限境界（#81、#83、#84、#86）。
3. 用途プリセット、縦/正方形出力、シーン編集、確認モード、全7画面、a11y（#75–#78、#89、#90）。
4. 出典/根拠/権利確認、字幕/読み/仕上げ（#79、#80）。
5. 複製/テンプレート/ゴミ箱/バックアップ、診断/公開資料、制作指標（#82、#88、#91）。
6. ネイティブ実出力回帰ハーネス・リリース前チェック（#65、#87）。モデル実API検証（#92）。
7. 差分レビュー、PR・GitHub反映、受け入れ条件を満たしたIssue/Projectのみ完了。

## 実画面検証

隔離プロファイル /tmp/newsvideo-implementation-check で、記事入力直後→設定へ移動→記事へ戻る、本文編集直後→ウィンドウを閉じる→新規ウィンドウで復元、をCUAで確認。ディスクのmanifestにも本文とrevisionを確認。台本入力の即時共有反映を確認後、Macがロックされたためパート切替・VoiceOver等の実画面検証は保留。

## 生成物の入力版（実装中）

記事・台本・プロンプト・画像スタイル/比率・音声スタイル・出力設定の指紋を保存。変更シーンだけ画像/音声を更新対象にし、旧動画の完成扱いを解除。実在ファイルをMain読込時と書出し前に検査。手動確認済み素材を維持するUIを追加。共有進捗計算で取り込み画像にAIプロンプトを要求しない。旧保存物は入力版が不明なため未確認扱い。123テスト、lint、型チェック通過。グローバルの生成モデル/音声設定の版と費用表示はジョブ基盤で接続予定。

## Mainジョブ・再試行・予算（実装中）

Main永続ジョブを追加し、記事画面から約750行の自動生成処理を除去。共有操作レジストリで既存生成処理を再利用。台本/プロンプト/シーン画像/シーン音声/最終動画ごとに成功結果を保存。キャンセルと入力変更が応答に競合しても結果をjob.outputsへ保全。再起動時のrunningはinterruptedへ移し自動課金しない。設定snapshotをAsyncLocalStorageでAPI処理へ適用。台本/素材の確認モード、予算チェック、生成前の見積幅、jobId別使用量、不明料金を保存。SDK内のOpenAI再試行のみとし、Geminiは共通の一時障害/429再試行、Retry-After、サービス別同時数制限へ移行。共有ストアには3-wayマージと競合時の明示的な解決操作を追加。133テスト通過（Mainジョブ全段階、取消→再開、レビュー停止、予算0、再起動復旧、再試行分類/待ち時間、並列制限、保存差分マージ）。型チェック通過。ジョブの実画面動作・実API・ネイティブ書出し検証は残る。
